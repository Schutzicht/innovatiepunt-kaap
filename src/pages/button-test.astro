---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Button Shape Test">
    <div class="min-h-screen bg-kaap-gray py-24 px-4">
        <div class="max-w-6xl mx-auto">
            <h1
                class="text-4xl font-bold text-white mb-8 font-heading text-center"
            >
                Button Shape Exploration
            </h1>
            <p class="text-gray-400 text-center mb-16 max-w-2xl mx-auto">
                Exploring different shapes to find the balance between "Not
                Round" and "Not Square", aligning with the "3 squares in a
                circle" brand identity.
            </p>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
                <!-- Option 1: Rounded Medium (Soft Square) -->
                <div
                    class="bg-white/5 p-8 rounded-xl border border-white/10 flex flex-col items-center gap-6"
                >
                    <h2 class="text-xl text-white font-bold">
                        1. Soft Square (Rounded-LG)
                    </h2>
                    <p class="text-sm text-gray-400 text-center">
                        Standard modern UI. Friendly but structured.
                    </p>
                    <button
                        class="px-8 py-4 bg-kaap-gradient text-white font-bold rounded-lg hover:scale-105 transition-transform shadow-[0_0_20px_rgba(234,44,152,0.3)]"
                    >
                        Ontdek KAAP
                    </button>
                    <button
                        class="px-8 py-4 border border-white/20 text-white font-bold rounded-lg hover:bg-white/10 transition-colors"
                    >
                        Kaap je M²
                    </button>
                </div>

                <!-- Option 2: Squircle (Super Ellipse-ish) -->
                <div
                    class="bg-white/5 p-8 rounded-xl border border-white/10 flex flex-col items-center gap-6"
                >
                    <h2 class="text-xl text-white font-bold">
                        2. Squircle (Rounded-2XL)
                    </h2>
                    <p class="text-sm text-gray-400 text-center">
                        Very modern, often used by tech brands (iOS). Not fully
                        round, not square.
                    </p>
                    <button
                        class="px-8 py-4 bg-kaap-gradient text-white font-bold rounded-2xl hover:scale-105 transition-transform shadow-[0_0_20px_rgba(234,44,152,0.3)]"
                    >
                        Ontdek KAAP
                    </button>
                    <button
                        class="px-8 py-4 border border-white/20 text-white font-bold rounded-2xl hover:bg-white/10 transition-colors"
                    >
                        Kaap je M²
                    </button>
                </div>

                <!-- Option 3: Cut Corner (Tech/Robust) -->
                <div
                    class="bg-white/5 p-8 rounded-xl border border-white/10 flex flex-col items-center gap-6"
                >
                    <h2 class="text-xl text-white font-bold">
                        3. Cut Corner (Custom)
                    </h2>
                    <p class="text-sm text-gray-400 text-center">
                        Aggressive, technical looking. Matches the
                        "Industrial/Robust" vibe.
                    </p>
                    <button
                        class="px-8 py-4 bg-kaap-gradient text-white font-bold hover:scale-105 transition-transform shadow-[0_0_20px_rgba(234,44,152,0.3)]"
                        style="clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);"
                    >
                        Ontdek KAAP
                    </button>
                    <button
                        class="px-8 py-4 border border-white/20 bg-white/5 text-white font-bold hover:bg-white/10 transition-colors"
                        style="clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);"
                    >
                        Kaap je M²
                    </button>
                </div>

                <!-- Option 4: Leaf/Asymmetric -->
                <div
                    class="bg-white/5 p-8 rounded-xl border border-white/10 flex flex-col items-center gap-6"
                >
                    <h2 class="text-xl text-white font-bold">
                        4. Asymmetric (Organic)
                    </h2>
                    <p class="text-sm text-gray-400 text-center">
                        Top-Left and Bottom-Right rounded. Others sharp. Matches
                        "Squares in Circle" tension.
                    </p>
                    <button
                        class="px-8 py-4 bg-kaap-gradient text-white font-bold rounded-tl-2xl rounded-br-2xl hover:scale-105 transition-transform shadow-[0_0_20px_rgba(234,44,152,0.3)]"
                    >
                        Ontdek KAAP
                    </button>
                    <button
                        class="px-8 py-4 border border-white/20 text-white font-bold rounded-tl-2xl rounded-br-2xl hover:bg-white/10 transition-colors"
                    >
                        Kaap je M²
                    </button>
                </div>

                <!-- Option 5: Block (Sharp) -->
                <div
                    class="bg-white/5 p-8 rounded-xl border border-white/10 flex flex-col items-center gap-6"
                >
                    <h2 class="text-xl text-white font-bold">
                        5. Block (Sharp)
                    </h2>
                    <p class="text-sm text-gray-400 text-center">
                        Pure square/rectangle. Very robust/brutalist.
                    </p>
                    <button
                        class="px-8 py-4 bg-kaap-gradient text-white font-bold rounded-none hover:scale-105 transition-transform shadow-[0_0_20px_rgba(234,44,152,0.3)]"
                    >
                        Ontdek KAAP
                    </button>
                    <button
                        class="px-8 py-4 border border-white/20 text-white font-bold rounded-none hover:bg-white/10 transition-colors"
                    >
                        Kaap je M²
                    </button>
                </div>
            </div>

            <div class="mt-16 text-center">
                <a href="/" class="text-gray-400 hover:text-white underline"
                    >Back to Home</a
                >
            </div>
        </div>
    </div>
</Layout>
